{
    "version": "0.2.0",
    "configurations": [
        {
            "args": ["${file}"],
            "cwd": "${workspaceFolder}",
            "isShellCommand": true,
            "name": "Testing the Liz Assembler",
            "preLaunchTask": "Run CMake and Build",
            "request": "launch",
            "program": "Build/bin/lizas",
            "type": "lldb",
        }
    ]
}