// Launch.json
// Not sure a better way to organize than just have a ton of test rows
{
    "version": "0.2.0",
    "configurations": [
        {
            "args": [""],
            "cwd": "${workspaceFolder}",
            "isShellCommand": true,
            "name": "Source/initial_vm.test.cc",
            "preLaunchTask": "runBuild",
            "program": "${workspaceFolder}/Build/Tests/test_initial_vm",
            "request": "launch",
            "type": "lldb",
        }
    ]
}

